{
    "name": "@autonoma/environment-factory",
    "version": "0.0.1",
    "private": true,
    "type": "module",
    "exports": {
        ".": "./src/index.ts"
    },
    "scripts": {
        "build": "tsc",
        "typecheck": "tsc --noEmit",
        "lint": "oxlint --fix src/ test/",
        "fmt": "oxfmt --write src/ test/",
        "test": "vitest run --passWithNoTests"
    },
    "dependencies": {
        "@autonoma/logger": "workspace:*",
        "@autonoma/types": "workspace:*"
    },
    "devDependencies": {
        "@types/node": "catalog:",
        "typescript": "catalog:",
        "vitest": "catalog:"
    }
}
