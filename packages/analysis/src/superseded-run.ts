/**
 * The failure reason a superseded run's `AnalysisJob` is closed with. Prose for an operator reading the row;
 * whether a run WAS superseded is `AnalysisJob.superseded`, never this string.
 */
export const SUPERSEDED_RUN_REASON = "Superseded by a newer analysis request";
