// Section slot for the adaptive side-by-side hero (failure screenshot with pin + video).
// Filled by a later slice of the bug-page redesign (#1269) once healing authors the
// report's `primaryScreenshot` + evidence manifest. Renders nothing until then.
export function BugHeroMedia() {
  return null;
}
